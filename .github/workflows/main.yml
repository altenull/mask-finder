name: Build
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: checkout source code
        uses: altenull/mask-finder@master

      - name: install Dependencies
        run: npm install

      - name: build
        run: npm run build
